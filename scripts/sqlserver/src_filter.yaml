allow:
- catalog: "${SRCDB_DB}"
  schema: "${SRCDB_SCHEMA}"
  types: [TABLE]
  allow:
    # all of the tables are scoped up automatically
    # THEUSERTABLE:
    # sbtest1:
    customer:
    district:
    # history -- cannot do realtime without primary key
    item:
    new_order:
    oorder:
    order_line:
    REPLICATE_IO_CDC_HEARTBEAT:
    stock:
    warehouse:
