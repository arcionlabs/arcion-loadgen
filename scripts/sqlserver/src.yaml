type: SQLSERVER

host: ${SRCDB_HOST}
port: 1433

username: ${SRCDB_ARC_USER}
password: ${SRCDB_ARC_PW}
database: ${SRCDB_ARC_USER}

max-connections: 4

max-retries: 1

# real-time and full only
extractor: CHANGE # {CHANGE|LOG}

