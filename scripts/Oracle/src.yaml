type: ORACLE

host: ${SRCDB_HOST}
port: 53547

service-name: IO

username: 'arcsrc'
password: 'password'

max-connections: 30
#max-fetch-schema-connections: 30  #default value same max-connections

max-retries: 10
retry-wait-duration-ms: 1000
continuous-log-mining: false     # for 12c, 18c, 19c, should set up as false, since not supported 
#continuous-log-mining: true
#ssl:
#   trust-store:
#       ssl-store-type: 'JKS'
#       path: '/path/to/truststore-file.jks'
#       password: 'changeit'
