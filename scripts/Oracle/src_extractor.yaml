snapshot:
   threads: 16
   fetch-size-rows: 10_000
   verify-row-count: false
   _fetch-exact-row-count: true
   _traceDBTasks: true
#  inf-number-behavior: EXCEPTION   # EXCEPTION, ROUND, CONVERT_TO_NULL
#  flashback-query: true
#  parallel-query: true
#   serialize-fetch-createSql: false
#   serialize-fetch-create-sql-no-constraints: false
#  fetch-user-roles: true

#   native-load:
#     enable: false
#     stage-type: SHARED_FS
#     directory: SHARED_STAGE
#     shared-path: path-to-nfs

#   per-table-config:
#   - schema: tpch
#     tables:
#       lineitem1:
#         row-identifier-key: [ORDERKEY, LINENUMBER]
#         extraction-priority: 1 #Higher value is higher priority. Both positive and negative values are allowed. Default priority is 0 if unspecified.
#       lineitem1:
#         row-identifier-key: [ORDERKEY, LINENUMBER]
#        products:
#          per-partition-config:
#          - partition-name: SYS_P461
#            row-count: 0
#          - partition-name: SYS_P462
#            row-count: 0
#          - partition-name: SYS_P463
#            row-count: 1
#          - partition-name: SYS_P464
#            row-count: 3
#        part:
#          row-count: 2000

realtime:
  threads: 4
  _traceDBTasks: true
  #idempotent-replay: NONE #[ALWAYS, NEVER, NONE]
  #oldest-active-txn-window-hr: 24
  #block-ddl-transaction: true
  #fetch-size-rows: 0
  #retention-period-min: 60
  #stop-inactive-lm-session-after-s: 2
  #auto-adjust-lm-timeout: false
  #from-scn : 2911523
  #to-scn :
  #from-ts :
  #to-ts :
  heartbeat:
    enable: true
    schema: "arcsrc"
    interval-ms: 10000

  #ddl-replication:
  #  enable: true                 # This config enables detection of DDLs fire on source. Supported values are true/false.
  #  ddl-replication-mode: INLINE # Default is REINIT mode. Supported modes REINIT and INLINE.
                                  # INLINE MODE : We will parse cdc logs generated by source and apply them to target.
                                  # If we are not able to parse ddl or target does not support a particular ddl then we fall back to the REINIT method and do reinitialization of that table.
                                  # REINIT MODE : After every x seconds (governed by detect-ddl-interval config below) we will fetch schemas from source and compare with target to detect DDLs.
                                  # Once the ddl is detected, the table needs to be reinitialized. If REINIT mode is used please set skip-tables-on-failures: true in applier config.
  #  detect-ddl-interval: 600     # This config sets the value of DDL detection interval in seconds E.g if set to 600 then DDLs will be detected after every 600 seconds.
                                  # This is only used when ddl-replication-mode is REINT. If DDLs are rare it is recommended to set large value.
  #  schema: "io_replicate"       # please create a log table and specify schema of that log table here. Used only when ddl-replication-mode is INLINE.
    
#  log-miner-dict-file: '/data/shared_fs/dictionary.ora'

#  start-position:
    #start-scn: 2362927

    #per-table-config:
    #- schema: tpch
    #tables:
    #  t1:
        #from-scn : 2539138
        #to-scn :
        #from-ts : '19-AUG-19 11.03.05.552248 AM'
        #to-ts :
