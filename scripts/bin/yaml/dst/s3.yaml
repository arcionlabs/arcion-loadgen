type: S3

access-key: ""
secret-key: ""

bucket: "bucket_name"
root: "root_dir"

stage:
  type: SHARED_FS
  root-dir: /home/user/stage #directory where CSV files will be staged before uploading to S3

# file-format: CSV
max-connections: 50
