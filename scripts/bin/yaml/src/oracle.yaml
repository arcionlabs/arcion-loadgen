type: ORACLE

host: localhost
port: 53546

service-name: IO

username: 'replicant'
password: 'Replicant#123'

max-connections: 30
#max-fetch-schema-connections: 30  #default value same max-connections

#network-compression: ON # ON|OFF|AUTO
#network-compression-threshold-size: 1024

#stage:
#  type: LOCAL_FS
#  root-dir: /data/shared_fs

max-retries: 10
retry-wait-duration-ms: 1000
#source-failover: true

#charset: AL32UTF8
