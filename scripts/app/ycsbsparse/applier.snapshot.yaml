{% if 'DSTDB_GRP'|getenv('') not in ('nullstorage','s3','') %}
snapshot:
  per-table-config:
  - catalog: {{'DSTDB_DB'|getenv('')}}
    schema: {{'DSTDB_SCHEMA'|getenv('')}}
    tables:
{% if 'DSTDB_GRP'|getenv('') == 's2' %}
      THEUSERTABLE{{'workload_size_factor_str'|getenv('')}}:
        table-store: column
      theusertable{{'workload_size_factor_str'|getenv('')}}:
        table-store: column
{% endif %}
{% endif %}