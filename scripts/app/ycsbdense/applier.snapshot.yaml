{% if 'DSTDB_GRP'|getenv('') not in ('nullstorage','s3','') %}
snapshot:
  per-table-config:
  - catalog: {{'DSTDB_DB'|getenv('')}}
    schema: {{'DSTDB_SCHEMA'|getenv('')}}
    tables:
{% if 'DSTDB_GRP'|getenv('') == 's2' %}
      DENSEABLE{{'workload_size_factor_str'|getenv('')}}:
        table-store: column
      densetable{{'workload_size_factor_str'|getenv('')}}:
        table-store: column
{% endif %}
{% endif %}