allow:
- catalog: "${SRCDB_DB}"
  schema: "${SRCDB_SCHEMA}" 
  types: [TABLE]
  allow:
    # all of the tables are scoped up automatically
    # tpcc customer table c_data column has CLOB that is not suported
    theusertable:
