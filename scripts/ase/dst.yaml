type: SYBASE_ASE

host: '${DSTDB_HOST}'
port: 5000

username: '${DSTDB_ARC_USER}'
password: '${DSTDB_ARC_PW}'
database: '${DSTDB_ARC_USER}'

transaction-store-location: ${CFG_DIR}/stage 

max-connections: 5

max-retries: 1
