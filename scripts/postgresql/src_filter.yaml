allow:
- catalog: ${SRCDB_DB}
  schema: ${SRCDB_SCHEMA}  
  types: [TABLE]
  allow:
    # pg will lowercase by default.
    theusertable:
    # tpcc schema not supported yet for full and real time
  block: [theusertable100, theusertable10, densetable, densetable10]
  unlisted: ALLOW
