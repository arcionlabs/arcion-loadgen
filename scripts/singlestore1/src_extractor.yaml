snapshot:
  threads: 16
  fetch-size-rows: 15_000

# min-job-size-rows: 1_000_000
# max-jobs-per-chunk: 32

  _traceDBTasks: true
#  fetch-user-roles: true

#  per-table-config:
#  - catalog: tpch
#    tables:
#      ORDERS:
#        num-jobs: 1
#      LINEITEM:
#        row-identifier-key: [L_ORDERKEY]
#        split-key: l_orderkey
#        split-hints:
#          row-count-estimate: 15000
#          split-key-min-value: 1
#          split-key-max-value: 60_000
#      ORDERS1:
#        row-identifier-key: [O_ORDERKEY]

delta-snapshot:
  #threads: 32
  #fetch-size-rows: 10_000

  #min-job-size-rows: 1_000_000
  max-jobs-per-chunk: 32
  _max-delete-jobs-per-chunk: 32

  delta-snapshot-key: ts
  delta-snapshot-interval: 10
  delta-snapshot-delete-interval: 10
  _traceDBTasks: true
  replicate-deletes: false

  per-table-config:
  - catalog: arcsrc
    tables:
      usertable:
        # NOTE: this field name is case sensitive
        row-identifier-key: [YCSB_KEY]
      sbtest1:
        row-identifier-key: [id]
