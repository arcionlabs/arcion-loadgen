type: MYSQL

# will be set by envsubst from menu.sh 
host: ${DSTDB_HOST:-mysql}
port: 3306

username: ${DSTDB_ARC_USER:-arcdst}
password: ${DSTDB_ARC_PW:-Password}

max-connections: 30
max-retries: 1
  
