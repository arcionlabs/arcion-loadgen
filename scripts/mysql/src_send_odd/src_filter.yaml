allow:
- catalog: "${SRCDB_ARC_USER}"
  schema: ${SRCDB_SCHEMA}  
  types: [TABLE]
  allow:
    # all of the tables are scoped up automatically
    t1:
      # only replicate odd number
      conditions: "id%2 = 1"
