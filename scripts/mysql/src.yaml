type: MYSQL

# will be set by envsubst from menu.sh
host: ${SRCDB_HOST}
port: 3306

username: ${SRCDB_ARC_USER}
password: ${SRCDB_ARC_PW}

# will be set by envsubst from menu.sh 
slave-server-ids: [ ${PID} ]
max-connections: 30

# Optional parameters with default values
# ddl-connection:
# id:null
# max-retries:20
# proxy-source:FALSE
# retry-wait-duration:10000
# source-failover:FALSE
# timeout-millis:30000
# xa-realtime-failure-strategy:null
# xa-resume-on-reconnection:null
# xa-snapshot-failure-strategy:null
