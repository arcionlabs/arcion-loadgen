allow:
- catalog: ${SRCDB_DB}
  schema: ${SRCDB_SCHEMA}
  types: [TABLE]
  allow:
$( for w in $( echo ${workload_modules_bb} | tr ',' '\n' ); do
  echo "- catalog: ${SRCDB_DB}_${w}"
  echo "  schema: ${SRCDB_SCHEMA}"
  echo "  types: [TABLE]"
  echo "  allow:"
done
)