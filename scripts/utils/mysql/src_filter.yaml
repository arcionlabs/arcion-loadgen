allow:
- catalog: "${SRCDB_DB}"
  schema: ${SRCDB_SCHEMA}  
  types: [TABLE]
  allow:
    # all of the tables are scoped up automatically
    #theusertable:
    #locktable:
    #order_line:
    #stock:
    #sbtest1:
    #checking:
    #added_tweets:
    #votes:
    #theusertable:
    #sitest:
    #district:
    #new_order:
    #subscriber:
    #oorder:
    #customer:
    #iotablesmallrow:
    #history:
    #warehouse:
    #savings:
    #useracct:
    #watchlist:
    #call_forwarding:
