realtime:
  per-table-config:
  - catalog: ${SRCDB_DB}
    schema: ${SRCDB_SCHEMA}   
    tables:
      USERTABLE:
        split-key: YCSB_KEY
        split-method: RANGE
        extraction-priority: -1 
      usertable:
        split-key: ycsb_key
        split-method: RANGE
        extraction-priority: -1         