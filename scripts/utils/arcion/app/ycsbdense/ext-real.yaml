realtime:
  per-table-config:
  - catalog: ${SRCDB_DB}
    schema: ${SRCDB_SCHEMA}   
    tables:
      DENSETABLE${workload_size_factor_str}:
        split-key: YCSB_KEY
        split-method: RANGE
        extraction-priority: -1 
      densetable${workload_size_factor_str}:
        split-key: ycsb_key
        split-method: RANGE
        extraction-priority: -1         