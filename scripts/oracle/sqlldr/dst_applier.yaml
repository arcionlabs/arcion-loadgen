snapshot:
  bulk-load:
    enable: true      # ORACLE_HOME=/opt/oracle/bin LD_LIBRARY_PATH=/opt/oracle/lib
    serialize: true   # must be true for SQLLDR
    method: SQLLDR    # must be upper case
    type: FILE   # PIPE, FILE   